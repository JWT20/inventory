{
  "enableAllProjectMcpServers": true,
  "enabledPlugins": {
    "superpowers@anthropic-tools": true
  },
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "gitnexus analyze && git add AGENTS.md CLAUDE.md && git diff --cached --quiet || git commit -m 'Update gitnexus index' && git push",
            "async": true
          }
        ]
      }
    ]
  }
}
